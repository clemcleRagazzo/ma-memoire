<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".MemoryApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/brain"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/brain_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaMemoire">
        <activity
            android:name="com.moulis.mamemoire.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MaMemoire">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver
            android:name="com.moulis.mamemoire.NotificationReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />

                <action android:name="ACTION_REPLY_EVENING" />
                <action android:name="ACTION_YES" />
                <action android:name="ACTION_NO" />
                <action android:name="MORNING_NOTIFICATION" />
                <action android:name="EVENING_NOTIFICATION" />
            </intent-filter>
        </receiver>
    </application>

</manifest>